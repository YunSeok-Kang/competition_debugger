name: Deploy on Push to Main

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code and restart server
        working-directory: /home/milab/cvpr2026cctv/team01/competition_debugger/submission_debugger
        run: |
          echo "=== Deploying new version ==="
          bash scripts/update_from_main.sh main
          echo "=== Deploy finished ==="
